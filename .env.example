# Copy this file to ".env" (same folder) and fill in your real values.
# ".env" itself is gitignored and will never be committed.

SCOUTAI_DB_URL=postgresql://USERNAME:PASSWORD@localhost:5432/scoutai_db
